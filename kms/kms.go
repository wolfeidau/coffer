package kms

// Decrypt decrypt the cipher text using the provided key.
func Decrypt(ct, key []byte) []byte {
	var buf []byte

	return buf
}

// Encrypt the plain text message using the provided key.
func Encrypt(plaintext, key []byte) []byte {
	var buf []byte

	return buf
}
